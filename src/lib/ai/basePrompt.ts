export const BASE_APP_PROMPT = `Design and implement a comprehensive real-time state persistence and recovery system for an application that continuously monitors, captures, and saves the complete current execution state to a designated checkpoint file. The system must automatically write operations to prevent data corruption and implement atomic file operations with write-ahead logging or temporary file staging followed by atomic rename to ensure consistency. Implement tracking and persistence of all critical state variables including execution position and progress markers, current operation being performed, pending tasks queue, configuration parameters, user context and session data, variable values and their current states, call stack information if applicable, in-progress transactions or operations requiring completion on restart, and any application-specific state that would be lost on termination. The persistence mechanism must operate asynchronously on a dedicated background thread or thread pool without blocking the main application workflow and should implement configurable debouncing or throttling mechanisms to balance between responsiveness and I/O performance, including batched writes and coalescing of rapid state changes within a configurable time window. Create a comprehensive state recovery mechanism that automatically triggers on application startup and reads the persisted checkpoint file to reconstruct the exact application state at the point of failure, including replay of incomplete transactions, restoration of user session context, repositioning to the exact execution point, and reconstruction of all pending operations. The recovery system must handle scenarios including but not limited to unexpected crashes, out-of-memory errors, token or resource exhaustion, manual termination, power failure, or any interruption that causes loss of in-memory state, and should implement graceful degradation when full recovery is impossible. Implement rigorous validation checks on the recovered state including checksum verification, schema validation, timestamp consistency checks, cross-reference validation between related state components, and detection of corrupted or truncated data, and if validation fails implement fallback procedures to a known good state, last valid checkpoint, or clean start with appropriate error reporting. Implement proper error handling for file I/O failures including retry logic with exponential backoff, disk space issues with alerts and cleanup procedures, and concurrent access scenarios using file locking mechanisms or lock-free data structures. Ensure thread-safety in all persistence and recovery operations using appropriate synchronization primitives, immutable state snapshots, and atomic operations, and implement proper locking hierarchy to prevent deadlocks. Provide detailed structured logging of all state save and recovery events including timestamps, state size, duration, success/failure status, and relevant context for debugging, auditing, and performance monitoring purposes. The system should support configurable checkpoint frequency based on time intervals, operation count, or significant state changes, and allow differentiation between critical state that must be preserved such as user data and transaction state versus optional state that can be recalculated or reset on restart such as cached computations or transient UI state, with separate storage mechanisms or prioritization for each category. Additionally, implement checkpoint lifecycle management including automatic pruning of old checkpoints beyond a configurable retention count or age, manual checkpoint creation capability for critical operations, and checkpoint metadata management for efficient recovery point selection.`;

export function buildAIPrompt(taskPrompt: string) {
    return `${BASE_APP_PROMPT}\n\n${taskPrompt}`.trim();
}
