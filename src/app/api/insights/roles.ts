/** Roles allowed to view the read-only KRA/attendance Insights (mirrors the
 * Performance Observatory + HR analytics audience). */
export const INSIGHTS_ROLES = ['SUPER_ADMIN', 'ADMIN', 'MANAGER', 'HR_MANAGER', 'HR', 'TEAM_LEADER'];
